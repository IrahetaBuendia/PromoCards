// Dashboard principal — requiere autenticación (ver middleware.ts)
export default function DashboardPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">PromoCards SV</h1>
      <p className="mt-2 text-gray-500">Dashboard — próximamente</p>
    </main>
  );
}
